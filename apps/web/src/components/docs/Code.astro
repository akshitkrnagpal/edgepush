---
interface Props {
  code: string;
}

const { code } = Astro.props;
---

<pre class="mt-4 overflow-x-auto border border-rule-strong bg-surface p-5 font-mono text-[13px] leading-[1.6] text-text"><code>{code}</code></pre>
